openapi: 3.1.0
info:
  version: 1.0.0
  title: Flats app
  license:
    name: Apache 2.0
    url: http://www.apache.org/licenses/LICENSE-2.0.html
  description:
    This is a documentation of flats app
tags:
  - name: Flats
    description: Operations about flats.
servers:
  - url: http://localhost:3000
  - url: https://flats-2.onrender.com
paths:
  /flats:
    get:
      $ref: ../swagger/paths/flats/get.yaml
    post:
      $ref: ../swagger/paths/flats/post.yaml
  /flats/{id}:
    get:
      $ref: ../swagger/paths/flats/{id}/get.yaml
    patch:
      $ref: ../swagger/paths/flats/{id}/patch.yaml
    delete:
      $ref: ../swagger/paths/flats/{id}/delete.yaml

components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
